 pipeline {
    agent any
     stages{
         stage('clone my code'){
               git 'https://github.com/dhumalparth/maven-project'
           }
           }
      stages{
         stage('compile my code'){
           setps {
               withMaven(maven : 'LocalMaven') {
                   sh 'mvn compile'
                   }
                   }
                   }
                   }
                   }
